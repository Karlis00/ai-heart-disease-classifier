# Copy this file to .env before running locally or building for production.

# Leave blank when running locally with Vite (the dev server proxy handles /predict → localhost:8000)
VITE_API_URL=

# Set this to your deployed backend URL for production builds, e.g.:
# VITE_API_URL=https://cardioscan-api.onrender.com
